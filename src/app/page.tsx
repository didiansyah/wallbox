import Link from "next/link";
import { ArrowRight, CheckCircle2, FileSearch, LockKeyhole, Network } from "lucide-react";
import { Header } from "@/components/landing/header";
import { DashboardPreview } from "@/components/landing/dashboard-preview";
import { HowItWorks } from "@/components/landing/how-it-works";

export default function Home(){return <main><Header/><section className="grid-bg relative overflow-hidden border-b border-border"><div className="mx-auto grid max-w-7xl gap-12 px-5 py-20 md:grid-cols-[1.05fr_.95fr] md:py-28"><div><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-border bg-secondary px-3 py-1 text-xs font-medium text-muted-foreground"><Network size={14}/>Walrus storage + Sui certificate + Tatum RPC</div><h1 className="max-w-3xl text-5xl font-semibold tracking-[-.04em] md:text-7xl">The black box for autonomous AI agents.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground">Wallbox captures prompts, tool calls, sources, outputs, and artifacts into verifiable audit capsules stored on Walrus and certified on Sui through Tatum.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link href="/run" className="inline-flex items-center justify-center gap-2 rounded-md bg-primary px-5 py-3 font-semibold text-primary-foreground">Run demo agent <ArrowRight size={18}/></Link><Link href="/verify/local-demo" className="inline-flex items-center justify-center rounded-md border border-border bg-secondary px-5 py-3 font-semibold">Verify certificate</Link></div><div className="mt-8 grid max-w-xl gap-3 text-sm text-muted-foreground sm:grid-cols-3"><span className="flex items-center gap-2"><CheckCircle2 size={16} className="text-primary"/>Canonical hashing</span><span className="flex items-center gap-2"><LockKeyhole size={16} className="text-primary"/>Tamper evidence</span><span className="flex items-center gap-2"><FileSearch size={16} className="text-primary"/>Public verifier</span></div></div><DashboardPreview/></div></section><section id="security" className="mx-auto max-w-7xl px-5 py-16"><div className="rounded-2xl border border-border bg-card p-8 md:p-10"><p className="text-sm font-semibold uppercase tracking-[.2em] text-primary">Problem</p><h2 className="mt-3 text-3xl font-semibold">AI agents act, but nobody can audit them.</h2><p className="mt-4 max-w-3xl leading-7 text-muted-foreground">Centralized logs are mutable. Wallbox separates evidence storage from certificate anchoring so a disputed run can be checked later by recomputing the capsule hash.</p></div></section><HowItWorks/><section id="use-cases" className="mx-auto max-w-7xl px-5 pb-20"><div className="grid gap-4 md:grid-cols-4">{['AI trading agents','Research agents','Coding agents','Enterprise workflows'].map((u)=><div key={u} className="border-l-2 border-primary bg-card p-5 shadow-sm"><h3 className="font-semibold">{u}</h3><p className="mt-2 text-sm text-muted-foreground">Show what the agent saw, called, produced, and certified.</p></div>)}</div></section></main>}
