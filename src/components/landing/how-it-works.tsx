import { Archive, Bot, Fingerprint, ShieldCheck } from "lucide-react";
const steps=[['Record',Bot,'Capture prompts, tool calls, sources, artifacts, and outputs.'],['Store',Archive,'Package evidence into one canonical JSON bundle on Walrus.'],['Certify',Fingerprint,'Anchor capsule hash and blob ID in a Sui certificate via Tatum.'],['Verify',ShieldCheck,'Recompute hashes later and detect tampering without trusting logs.']];
export function HowItWorks(){return <section id="product" className="mx-auto max-w-7xl px-5 py-20"><div className="mb-10 max-w-2xl"><p className="text-sm font-semibold uppercase tracking-[.2em] text-primary">How it works</p><h2 className="mt-3 text-3xl font-semibold md:text-5xl">From agent run to verifiable evidence.</h2></div><div className="grid gap-4 md:grid-cols-4">{steps.map(([title,Icon,body])=><div key={String(title)} className="rounded-2xl border border-border bg-card p-5 shadow-sm"><Icon className="mb-5 text-primary" size={24}/><h3 className="mb-2 font-semibold">{String(title)}</h3><p className="text-sm leading-6 text-muted-foreground">{String(body)}</p></div>)}</div></section>}
