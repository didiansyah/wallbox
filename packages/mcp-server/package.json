{
  "name": "@wallbox/mcp-server",
  "version": "0.1.0",
  "description": "MCP stdio server for Wallbox audit capsule capture and verification.",
  "type": "module",
  "bin": {
    "wallbox-mcp-server": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/chmod-bin.mjs",
    "start": "node dist/index.js",
    "test": "vitest run src/**/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@wallbox/sdk": "workspace:*",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest"
  }
}
